# Frontend API base
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Core backend integration (used by the server; keep the real token in server/.env)
CORE_API_BASE_URL=https://tinsmith-manned-cleaver.ngrok-free.dev
CORE_CASES_ENDPOINT=/api/case
CORE_API_TOKEN=
